[project]
name = "vlm-scene-analyser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Pillow",
    "ipykernel>=7.2.0",
    "matplotlib",
    "pandas",
    "requests",
]
